# Required packages for ghcr.io/hypertidy/gdal-r-full
# Build FAILS if any of these fail to install or load.
# Format: "pkg" for CRAN, "owner/repo" for GitHub HEAD.

# gdalraster is the primary test target for this CI infrastructure.
# Use GitHub HEAD to catch breaking GDAL API changes before CRAN release.
firelab/gdalraster
