# R packages installed in ghcr.io/hypertidy/gdal-r
# Keep this list minimal — packages here are compiled in every image rebuild.
# Application-specific or GDAL-linking packages go in gdal-r-full.

# Package management
remotes
devtools
roygen2

# Testing frameworks
testthat
tinytest

# Check tooling
rcmdcheck
spelling

# Documentation
knitr
rmarkdown

# Compiled dependency bases — build these once here so derived images
# don't recompile them from scratch every time
Rcpp
cpp11

# Spatial primitives (no GDAL linkage, pure R/C++)
wk
s2
