# R packages for ghcr.io/hypertidy/gdal-r-full image
# Core GDAL-dependent packages for the R geospatial ecosystem

# Primary GDAL packages (the CRAN-5 we test)
gdalraster
terra
sf
vapour
stars

# gdalcubes (uses GDAL via its own mechanism)
gdalcubes

# units is needed by sf and stars
units
