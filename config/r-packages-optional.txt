# Optional packages for ghcr.io/hypertidy/gdal-r-full
# Build CONTINUES if any of these fail — failures are logged and summarised.
# These are the canary targets: failures here signal API changes worth reporting.
# Format: "pkg" for CRAN, "owner/repo" for GitHub HEAD.

# GitHub HEAD for all — we want to catch compile failures against latest GDAL
# before they hit CRAN users
sf
terra
vapour
gdalcubes
